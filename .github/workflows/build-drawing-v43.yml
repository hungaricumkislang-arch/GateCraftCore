name: Build GateCraftDrawing v2.1 AIX

on:
  push:
    branches: [ gatecraft-drawing-v43 ]
    paths:
      - 'drawing_extension/src/**'
      - 'drawing_extension/patch_share_v43.py'
      - '.github/workflows/build-drawing-v43.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout GateCraftCore
        uses: actions/checkout@v4
      - name: Set up Java 8
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '8'
      - name: Install Ant
        run: |
          sudo apt-get update
          sudo apt-get install -y ant unzip
      - name: Patch GateCraftDrawing share runtime
        run: python3 drawing_extension/patch_share_v43.py drawing_extension/src/com/gatecraft/drawing/GateCraftDrawing.java
      - name: Get official MIT App Inventor extension template
        run: git clone --recurse-submodules https://github.com/mit-cml/extension-template.git buildenv
      - name: Install GateCraftDrawing source
        run: |
          rm -rf buildenv/src/*
          cp -R drawing_extension/src/. buildenv/src/
      - name: Build AIX
        run: |
          cd buildenv
          ant clean extensions
      - name: Validate AIX metadata/runtime
        run: |
          set -euo pipefail
          mkdir -p dist /tmp/gcdraw43
          mapfile -t AIXS < <(find buildenv/out -maxdepth 2 -type f -name '*.aix' -print)
          if [ "${#AIXS[@]}" -ne 1 ]; then echo "Expected 1 AIX, got ${#AIXS[@]}"; exit 1; fi
          cp "${AIXS[0]}" dist/GateCraftDrawing.aix
          unzip -t dist/GateCraftDrawing.aix
          unzip -q dist/GateCraftDrawing.aix -d /tmp/gcdraw43
          grep -R -q 'CreateAndShareDrawingPDF' /tmp/gcdraw43
          grep -R -q 'GateCraftDrawing' /tmp/gcdraw43
          echo 'GateCraftDrawing v2.1 AIX PASS'
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: GateCraftDrawing-v2_1-aix
          path: dist/GateCraftDrawing.aix
          if-no-files-found: error
